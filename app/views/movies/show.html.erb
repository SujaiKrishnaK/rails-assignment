<div style=margin:20px>
 <div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold"><%= @movie.name %></h1>
        <p class="col-md-8 fs-4">Rating:<%= @movie.rating %> </p>
         <p class="col-md-8 fs-4">Language:<%= @movie.language %> </p>
         <%= link_to 'Edit', edit_movie_path(@movie), class:"btn btn-outline-success" %> 
          <%= link_to 'Back', movies_path, class:"btn btn-outline-danger" %>
      </div>
    </div>
</div>